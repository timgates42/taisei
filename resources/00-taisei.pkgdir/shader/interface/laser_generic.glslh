
#ifndef I_LASER_GENERIC_H
#define I_LASER_GENERIC_H

#include "../lib/defs.glslh"

#ifdef VERT_STAGE
ATTRIBUTE(0) vec2 a_position;
ATTRIBUTE(1) vec2 a_texCoord;
ATTRIBUTE(2) int a_laserID;
#endif

#ifdef FRAG_STAGE
OUT(0) vec4 o_fragColor;
#endif

flat VARYING(0) int v_laserID;
VARYING(1) vec2 v_texCoord;
VARYING(2) vec2 v_texCoordRaw;
VARYING(3) vec4 v_color;

struct Laser {
	vec4 color;
	vec4 textureRegion;
};

UNIFORM(0) sampler2D u_texture;
UNIFORM(1) Laser u_lasers[32];

#endif
